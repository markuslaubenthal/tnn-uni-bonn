Authors: Markus Laubenthal, Lennard Alms

execute with:

  python tnn.py

reads file train.dat and trains on the dataset
it will set the centers uniformly on the input space and set random weights
the centers stay fixed
the weights are trained such that the error minimizes.
